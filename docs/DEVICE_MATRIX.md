# Supported devices & API matrix

| Family | Devices | Local API | Monitoring | Controls | Notes |
|---|---|---|---|---|---|
| ESP-Miner / AxeOS official v2.x | Bitaxe Max (202), Ultra (204), Supra (400), Gamma (600), Gamma Turbo, Supra Hex, and other AxeOS/dual-chip boards | `GET /api/system/info`, `GET /api/system/asic`, `PATCH /api/system`, `POST /api/system/restart` | ✅ | ✅ Reboot, primary-pool change, fan (auto/manual), tune limited to firmware-approved option lists, with audit + rollback | Endpoints verified against tagged firmware source (v2.14.2, v2.15.1) and real devices. v2.15+ pool edits echo the `"*****"` password mask (omitting it wipes the stored password — verified in source, locked by test) |
| NerdQAxe (fork) | NerdQAxe++, NerdQAxe+, NerdQAxe+ Hydro | `GET /api/system/info`, `GET /api/system/asic`, `POST /api/system/restart` | ✅ | ✅ **Reboot** (`/api/system/restart`, inherited unchanged from ESP-Miner). Pool/fan/tune stay off (its `autofanspeed` is a mode enum, not a bool; those semantics unverified on the fork) | Real device fixture recorded |
| ESP-Miner forks | Lucky Miner-style firmware ("2.0.0" line) | `GET /api/system/info` only | ✅ | ❌ Monitoring-only until verified | Real fixture recorded; adapter defensively refuses control calls |
| Canaan Avalon Nano 3 | Nano 3 (verified live: fw 24071801, cgminer 4.11.1, API 3.7) | CGMiner TCP API on port 4028 — read commands `version`/`summary`/`estats`/`pools`/`coin`, and privileged `ascset` for controls | ✅ | ✅ **Pause/Resume** (`ascset 0,softoff`/`softon`, verified live, reversible) and **Reboot** (`ascset 0,reboot,0`; a wrong keyword is a harmless no-op). Fan/worklevel/frequency intentionally NOT exposed — arg ranges unverified. Pool changes need the web UI (unverified). | Firmware advertises its own ascset options via `ascset 0,help`; privileged API access confirmed from the LAN. `coin` supplies live network difficulty. `estats` MM fields: TMax/TAvg chip temps, Fan1 RPM/FanR%, PS[5] wall watts (REPORTED), WORKLEVEL mode, HDNA serial |
| Canaan Nano 3S / Avalon Q / Avalon Mini 3 | Nano 3S, Avalon Q, Avalon Mini 3 | Same CGMiner API family; accepted when `version` identifies as Avalon | Monitoring (compat-gated) | ✅ Pause/Resume + Reboot via the same `ascset` path as the Nano 3 (the Canaan adapter offers them to any Avalon device) — unverified on these specific models until one is reachable | The tolerant parser + control adapter treat them like the Nano 3 once reachable |
| Demo | Synthetic | — | ✅ (demo mode only) | — | Never mixed with real miners |
| Braiins OS (BOSer) | Braiins Mini Miner BMM 100 (verified live: boser-openwrt 0.1.0, API 3.7) | CGMiner-compatible TCP API on 4028: `version`, `summary`, `devs`, `temps`, `fans`, `devdetails`, `pools` (read) + `pause`/`resume` (control) | ✅ | ✅ **Pause/Resume** via BOSminer `{"command":"pause"}`/`{"command":"resume"}` (reversible; per Braiins' documented API — confirm on your unit). Pool edits are NOT exposed (Braiins documents switchpool/enablepool as not fully implemented — reset after restart); reboot/fan/tuning live in the unverified gRPC/web API | `devs` "Nominal MHS" supplies firmware-declared expected hashrate (attainment works out of the box). No power sensor on the BMM 100 → power UNAVAILABLE; no MAC/serial over this API → identity falls back to model+IP. Braiins OS+ conversions identifying as BOSer are accepted but unverified |
| Stock Bitmain (BMMiner) | Antminer S21 Pro (verified live); S21+, S21+ Hyd, S21 Hyd, S21 XP, S19 series, S17 series (same BMMiner API, compat-gated) | cgminer TCP API on 4028: `version`/`summary`/`stats`/`pools` | ✅ Hashrate (GHS), expected (total_rateideal), chip temps, fans, frequency, ASIC count, shares, uptime, pool | ✅ **Reboot** via the authenticated web CGI (`GET /cgi-bin/reboot.cgi`, HTTP Digest as `root` + stored password). Pool change needs the `set_miner_conf.cgi` config round-trip — not exposed until verified. No pause/resume on stock. | Verified against a real S21 Pro (BMMiner 1.0.0): chip temps from temp2_*/temp_chip*, fans fan1..fan_num, expected from total_rateideal. **Power is not in this API** → shown as unavailable (Bitmain reports wall power only via its web UI). Bitmain uses `GHS` not `MHS` |
| VNish | Antminer S21 Pro verified live (Vnish 1.3.4); Antminer forks | cgminer TCP API on 4028 (read) + VNish web API on 80 (control) | ✅ Hashrate (GHS), expected (total_rateideal), chip temps, fans, frequency, ASIC count, shares, uptime, pool, **and wall power** | ✅ **Reboot** + **Pause/Resume** via the VNish web API (`POST /api/v1/unlock` → Bearer token, then `/api/v1/system/reboot`, `/api/v1/mining/pause`/`resume`). Needs the miner's web password (stored encrypted). Pool/preset/fan changes go through the `/api/v1/settings` object round-trip — not exposed until verified | Verified against a real S21 Pro (Vnish 1.3.4). VNish's `version` errors (`Invalid command`) → family detected from the `stats` Type string ("Antminer S21 Pro (Vnish 1.3.4)"). Chip temps from temp2_*/temp3_*/temp_chip*, fans fan1..fan_num, freq from freq_avg*, expected from total_rateideal. **Unlike stock Bitmain, VNish reports per-chain wall power** via `chain_consumption*` (summed → REPORTED), so efficiency (J/TH) is computed |
| LuxOS | Antminer forks | Standard cgminer `version`/`summary`/`pools` (read) + session API on 4028 (control) | ✅ Basic (hashrate, shares, uptime, pool) | ✅ **Pause/Resume** via `logon`→SessionID then `curtail <session>,sleep`/`wakeup` (no password). Reboot targets a board index (whole-miner semantics unverified) and pool changes are deferred | Detected by `version`; temps/fans/power pending capture from a real unit. Declines Avalon/BOSer |
| WhatsMiner (MicroBT) | M2X/M3X/M5X/M6X families | BTMiner API on 4028 — JSON `{"cmd":"summary"}`/`pools`/`get_miner_info` (read commands are open) | ✅ Hashrate (MHS→GHS), shares, uptime, pool, chip temperature, fans (in/out), and power where the firmware reports it | ❌ Controls need MicroBT's encrypted admin-token API (unverified) | **Compat-gated: implemented against MicroBT's documented API, pending confirmation on a physical unit.** Detected via the `btminer` STATUS description. Write/token API intentionally not implemented |
| FutureBit | Apollo BTC (Gen1/Gen2) | cgminer TCP API on 4028 (`version`/`summary`/`pools`) | ✅ Basic (hashrate, shares, uptime, pool) via the generic cgminer path; recognized as "FutureBit" by its version strings | ❌ Controls use the Apollo's own HTTP dashboard API — not verified | Compat-gated; nominal hashrate/power seeded from the model spec registry when the device doesn't report expected |
| Generic cgminer (long tail) | Any miner exposing the standard cgminer API on 4028 (older Antminers, ePIC/Hiveon-flashed units, etc.) | cgminer `version`/`summary`/`pools` | ✅ Basic (hashrate, shares, uptime, pool) — accepted as "Generic ASIC (cgminer)" when the family isn't specifically recognized | ❌ None (unverified firmware) | The generic adapter now accepts unrecognized cgminer devices for monitoring instead of declining them; declines only Avalon/BOSer (owned by specific adapters) and non-cgminer hosts |

## Pending hardware verification (gated write paths)

These write paths are **deliberately not implemented** until each can be verified against a
real device — consistent with the project rule to never invent or guess an endpoint. The
transports are understood; what's missing is a confirmed request/response schema and a
safe-value check on hardware. Each stays capability-gated OFF (the UI shows it as
unsupported) until then.

| Gated action | Family | Intended path | Why it's gated | To unlock |
|---|---|---|---|---|
| **Pool change** | VNish | `GET /api/v1/settings` → mutate pools → `POST /api/v1/settings` (Bearer from `/unlock`) | The settings object is large and firmware-version-specific; a wrong round-trip could wipe unrelated config or passwords | Capture a real `GET /settings` from a unit, diff a pools-only edit, confirm the password-mask behavior, lock with a payload test |
| **Pool change** | Stock Bitmain | `POST /cgi-bin/set_miner_conf.cgi` (HTTP Digest as root) | Full miner-conf POST; an incomplete body can brick pool/fan config. Reboot already verified, pool change is not | Capture `get_miner_conf.cgi` from a real S21, round-trip a pools-only change, verify persistence + reboot behavior |
| **Firmware OTA** | Bitaxe / AxeOS | `POST /api/system/OTA` (+ `OTAWWW`) with the firmware binary | Destructive and irreversible if interrupted; must confirm the multipart shape, size limits, and progress/verify semantics from ESP-Miner source **and** a spare board | Verify the endpoint against tagged ESP-Miner source, test on a sacrificial Bitaxe, add a strong in-app confirmation + failure recovery guidance |
| **Mass firmware upgrade** | fleet (per family) | fan-out of each family's verified OTA (AxeOS above; Antminer/VNish/Braiins upgrade endpoints unverified) | A fleet-wide flow can only be as safe as the single-device OTA it repeats — none is verified end-to-end yet, and a bad push bricks many units at once | Verify each family's single-device OTA first, then add a staged/canary fleet flow (one unit, confirm healthy, continue) with strong confirmation |
| **Batch static-IP / network config** | Antminer-class (BTC-Tools-style) | vendor network-config endpoints (Bitmain `set_network_conf`-style CGI; others unknown) | Writes IP/gateway/DNS — a wrong value silently drops a miner off the network with no easy in-app recovery | Capture each family's `get`/`set` network-config round-trip from a real unit, verify persistence and a safe rollback path, gate behind an explicit confirmation |

When a unit is available, each becomes a normal capability-gated control with the standard
confirm → audit → (where possible) rollback flow.

## ESP-Miner endpoint & field notes

- `GET /api/system/info` — sole endpoint used in v1. Response size observed 54–114 keys
  depending on firmware.
- Field variations handled: `bestDiff` numeric **or** suffixed string; `vrTemp`
  null/0/absent; `expectedHashrate` absent on older firmware and forks; `boardVersion`
  absent on some builds; forks add `DeviceModel` + `sn_str` (used for identity).
- Power on Bitaxe boards is measured by an onboard sensor → surfaced as MEASURED.
- Fixtures: `app/src/test/resources/fixtures/espminer/` (real captures are redacted:
  MAC, SSID, wallet/worker, pool host).
