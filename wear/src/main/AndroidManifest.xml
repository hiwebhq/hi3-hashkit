<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Tiles use Play services; no internet permission — this app only reads the
         phone-supplied summary over the local Wearable Data Layer. -->
    <uses-permission android:name="com.google.android.wearable.permission.BIND_TILE_PROVIDER" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_wear"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_wear"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Companion app: relies on the paired phone app for data. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Glanceable fleet tile. -->
        <service
            android:name=".FleetTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/ic_wear" />
        </service>

        <!-- Receives fleet-summary updates from the phone and refreshes the tile. -->
        <service
            android:name=".FleetDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/hi3/fleet_summary" />
            </intent-filter>
        </service>
    </application>
</manifest>
